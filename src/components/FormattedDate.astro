---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('zh-TW', {
			year: 'numeric',
			month: '2-digit',
			day: 'numeric',
		})
	}
</time>
